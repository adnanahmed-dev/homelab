# ==========================================
# Terraform / OpenTofu
# ==========================================
.terraform/
# Specifically ignore your Docker plugin cache
terraform/data/

*.tfstate
*.tfstate.*
crash.log
crash.*.log
# Keep tfvars ignored if you use them for secrets, 
# but remember we moved your "blueprint" to terraform.tfvars.
# If you want to commit the blueprint, change this to !terraform.tfvars
*.tfvars
*.tfvars.json

.terraform.tfstate.lock.info
.terraformrc
terraform.rc
*.dot
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# ==========================================
# Ansible
# ==========================================
*.retry
.ansible_vault_pass
# Ignore local logs and facts cache
ansible/ansible.log
ansible/facts_cache/
# Ignore roles/collections downloaded from Galaxy (keep your own!)
roles/external/
collections/

# ==========================================
# Kubernetes / Helm / ArgoCD
# ==========================================
.kube/
kubeconfig
kubeconfig.yaml
# ArgoCD / Helm local dependencies
charts/*.tgz
charts/charts/
.helm/

# ==========================================
# Monitoring (Grafana / Prometheus)
# ==========================================
# Ignore local database storage if running via Docker
grafana/data/
prometheus/data/
alertmanager/data/

# ==========================================
# Networking & VPN (Tailscale)
# ==========================================
# Tailscale state and local socket files
.tailscale/
tailscale.state
tailscale.sock

# ==========================================
# CI/CD (Jenkins / GitLab Runner)
# ==========================================
# Jenkins workspace and secret files
.jenkins/
jenkins.war
# GitLab runner config and build logs
.gitlab-runner/
builds/
artifacts/

# ==========================================
# Secrets and Environment Variables
# ==========================================
.env
.env.*
secrets.yml
*.pem
*.key
*.pub
id_rsa*
# Track the example but never the real ones
!.env.example

# ==========================================
# OS / Editors
# ==========================================
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo